import 'package:flutter/material.dart';

// Shown on app start when Bloc is determening if user is logged in
class SpashPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("ANTHEM"),
      ),
    );
  }
}
